class Untitled:

    def __init__(self):
	self.coins=[25,10,5,1]
	self.total=100

    def answer(self):
	result=[]
	for c in self.coins:
		while(sum(result)<=self.total):
			result.append(c)
		if(sum(result)==self.total):print result

	return 9
	
		


